[pytest]
# Configuration unifiée — source unique de vérité pour pytest
# Les paramètres dans pyproject.toml [tool.pytest.ini_options] sont ignorés car ce fichier a priorité
addopts = -q --tb=short -p no:qt
testpaths = tests
