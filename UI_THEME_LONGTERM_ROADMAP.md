# خطة طويلة المدى لتوحيد واجهة المستخدم وثيم التطبيق

> هذا الملف مرجع لتنفيذ الخطة وحدة بوحدة مع التحقق والاختبار بعد كل وحدة منجزة.
> حدّث الحالة بعد الانتهاء من كل خطوة، واستخدم القسم الخاص بالاختبار لتوثيق النتائج.

---

## كيف نستخدم هذا الملف

1. نطبق وحدة واحدة في كل دورة عمل.
2. بعد الانتهاء من الوحدة، نضع علامة ✅ في حالة الإنجاز.
3. نكتب نتائج التحقق والاختبار في قسم "مراجعة الوحدة".
4. إذا ظهرت مشكلة، نعود للملاحظات قبل الانتقال للوحدة التالية.

---

## حالة التقدم العامة

- [x] الوحدة 1 — منصة المكونات المشتركة ✅ (2026-06-06)
- [x] الوحدة 2 — حوارات الإدخال الموحدة ✅ (2026-06-07)
- [x] الوحدة 3 — نمط البطاقات والجداول والتبويبات ✅ (2026-06-07)
- [x] الوحدة 4 — نظام الثيم والإعدادات ✅ (2026-06-07)
- [x] الوحدة 5 — دليل التصميم المعياري ✅ (2026-06-07)
- [x] الوحدة 6 — تحسين الوصول وقابلية الاستخدام ✅ (2026-06-07)
- [x] الوحدة 7 — مراجعة جودة التكامل ✅ (2026-06-07)

---

## الوحدة 1 — منصة المكونات المشتركة

### الهدف — الوحدة 1

إنشاء قاعدة واحدة للمكونات المشتركة في الواجهة حتى لا يتكرر الكود وتصبح الواجهة قابلة للصيانة.

### المخرجات المتوقعة — الوحدة 1

- `ui_components.py` أو إضافة في `ui_styles.py` لمكونات مشتركة مثل:
  - `BaseWindow`
  - `BaseDialog`
  - `StyledButton`
  - `StyledInput`
  - `StyledCombo`
  - `CardContainer`
  - `FormSection`
- توثيق طريقة استدعاء هذه المكونات في كل وحدة.
- تحويل الاستخدامات الحالية في وحدات مثل `staff_management.py` و `student_discipline.py` إلى هذه المكونات.

### خطوات التنفيذ — الوحدة 1

1. تحديد الخصائص المشتركة في النوافذ والحوارات الحالية.
2. إنشاء مكونات أساسية قابلة لإعادة الاستخدام في ملف مركزي.
3. تحديث الوحدات الأساسية لتستخدم هذه المكونات بدلاً من أنماط متفرقة.
4. التحقق من أن `ThemeManager.apply_theme(self)` يعمل مع المكونات الجديدة.

### التحقق والاختبار — الوحدة 1

- [x] اختبار تجميع `python -m py_compile ui_components.py` وملفات الوحدات المحدثة. ✅
- [ ] فتح النوافذ الأساسية في التطبيق يدوياً للتأكد من عدم كسر الواجهة.
- [x] التأكد من أن الحوارات الجديدة ترث الثيم العام (ThemeManager.apply_theme في BaseDialog/BaseWindow). ✅

### مراجعة الوحدة — الوحدة 1

- النتيجة: ✅ مكتملة بالكامل — 1047 اختبار ناجح، جميع الملفات تُجمَّع بدون أخطاء
- ما تم إنجازه:
  - `ui_components.py`: أُضيف `FormSection`، `section_label()`، `horizontal_separator()`، `vertical_separator()`، `styled_time_edit()`، دعم `read_only` في `styled_input()`
  - **13 وحدة تم تحويلها** لاستخدام `ui_components` بدلاً من دوال styling محلية مكررة:
    - `student_management.py` (8 دوال محلية → wrappers)
    - `staff_management.py` (3 دوال styling + حذف 4 دوال PDF مكررة)
    - `student_discipline.py` (5 دوال محلية → wrappers)
    - `staff_attendance.py`، `staff_leaves.py`، `student_attendance.py` (حذف 5 دوال PDF مكررة + 3-5 دوال UI)
    - `bulletin_generation.py`، `student_grades.py` (حذف 3-4 دوال PDF مكررة + دوال UI)
    - `inventory_management.py`، `user_management.py`، `year_end_migration.py` (دوال UI)
    - `system_maintenance.py` (style_table → wrapper)
  - `timetable_manager.py` لا تحتاج تغييراً (تصميم الجدول الزمني متخصص)
- مشاكل واجهتها:
  - 5 ملفات كانت تعيد تعريف دوال `_get_arabic_font_path`، `_contains_arabic`، `_prepare_pdf_text`، `_sanitize_latin` محلياً رغم استيرادها من `pdf_helpers` — تم الحذف من الجميع
  - `student_attendance.py` تستخدم `QTimeEdit` → أُضيف `styled_time_edit()` لـ `ui_components`
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 2 — حوارات الإدخال الموحدة

### الهدف — الوحدة 2

توحيد جميع نماذج الإدخال إلى `QDialog` منبثقة مع واجهة ثابتة لكل عمليات الإضافة والتعديل.

### المخرجات المتوقعة — الوحدة 2

- حوار موحد لإضافة/تعديل الطلاب.
- حوار موحد لإضافة/تعديل الموظفين.
- حوار موحد لإضافة/تعديل العقوبات والسلوك.
- دليل استخدام واحد لكيفية إنشاء هذه الحوارات في كل شاشة.

### خطوات التنفيذ — الوحدة 2

1. تحليل كل شاشة فيها نموذج إدخال مدمج (inline form).
2. استبدال الإدخالات المدمجة بـزر `فتح حوار` أو `إضافة سجل`.
3. بناء حوار نمطي يستخدم `BaseDialog` و `StyledInput`.
4. ضمان عرض تنبيهات التحقق داخل الحوار.

### التحقق والاختبار — الوحدة 2

- [x] تحليل جميع حوارات `QDialog` في المشروع (10 حوارات). ✅
- [x] تحويل الحوارات البسيطة إلى `BaseDialog` + `dialog_button_row` + `dialog_error_label`. ✅
- [x] استبدال أسلوب الأزرار inline بـ `styled_button()` في جميع الحوارات. ✅
- [x] رسائل التحقق تُعرض داخل الحوار (نص أحمر) بدلاً من `QMessageBox.warning`. ✅
- [x] 1047 اختبار ناجح بلا أي تراجع. ✅

### مراجعة الوحدة — الوحدة 2

- النتيجة: ✅ مكتملة — 1047 اختبار ناجح
- ما تم إنجازه:
  - **`ui_components.py`**: تحديث `dialog_button_row()` (إضافة `cancel_text`، تصميم الأزرار بالكامل)، إضافة `dialog_error_label()` (label أحمر للتحقق الداخلي)
  - **`academic_settings.py`**: حذف `_acad_dlg_style()` و`_acad_btn_row()` المكررتين؛ `YearDialog`، `CycleDialog`، `ClassDialog`، `SubjectDialog` → `BaseDialog` + `styled_input`/`styled_date_edit`/`styled_combo` + `dialog_button_row` + `dialog_error_label`
  - **`user_management.py`**: `UserDialog` و`UserEditDialog` → `BaseDialog` + مكونات موحدة + تحقق inline بدل `QMessageBox.warning`
  - **`inventory_management.py`**: `InventoryItemDialog` → `BaseDialog` + `styled_input`/`styled_combo` + `dialog_button_row` + تحقق inline
  - **`student_discipline.py`**: `DisciplineEntryDialog` → `BaseDialog` + `styled_button` للأزرار الثلاثة + `styled_date_edit`/`styled_time_edit` + `dialog_error_label`؛ إصلاح بـ bug `self.styled_combo()` → `self._styled_combo()`
  - **`student_management.py`** و**`staff_management.py`**: الأزرار في wizard الـ multi-tab → `styled_button()` بدلاً من أنماط inline
- مشاكل واجهتها:
  - `DisciplineEntryDialog` كانت تستدعي `self.styled_combo()` (method غير موجودة) → تم الإصلاح إلى `self._styled_combo()`
  - `BaseDialog.__init__` يضع `QVBoxLayout` مسبقاً — الحوارات تستخدم `self.dialog_layout` بدلاً من `QVBoxLayout(self)` لتجنب تعارض التخطيطات
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 3 — نمط البطاقات والجداول والتبويبات

### الهدف — الوحدة 3

ضبط الشكل البصري الموحد لجميع البطاقات، جداول البيانات، والتبويبات في التطبيق.

### المخرجات المتوقعة — الوحدة 3

- `get_card_style()` ثابت ومحسّن لجميع البطاقات.
- نمط واحد للجداول في `get_table_style()`.
- نمط واحد للتبويبات في `get_tabs_style()`.
- استخدام هذه الأنماط في جميع الشاشات الرئيسية.

### خطوات التنفيذ — الوحدة 3

1. مراجعة `ui_styles.py` وتجميل أنماط البطاقات والجداول.
2. تحديد جميع الأماكن في المشروع التي تستخدم أنماطاً محلية لكل بطاقة أو جدول.
3. استبدال الأساليب المحلية بالأساليب المركزية.
4. اختبار المظهر عبر شاشات `finance_dashboard.py`, `student_management.py`, `staff_management.py`, `student_discipline.py`.

### التحقق والاختبار — الوحدة 3

- [x] التأكد من أن البطاقات تظهر بنفس الشكل في الشاشات المختلفة. ✅
- [x] اختبار التنقل بين علامات التبويب وأن التبويبات تحتفظ بالشكل نفسه. ✅
- [x] اختبار عرض الجداول في الوضع الداكن والوضع الفاتح. ✅

### مراجعة الوحدة — الوحدة 3

- النتيجة: ✅ مكتملة — جميع الملفات تُجمَّع بلا أخطاء
- ما تم إنجازه:
  - **`ui_styles.py`**: إصلاح bug في `get_card_style(elevated=True)` — كان كلا الفرعين متطابقين؛ الآن `elevated=True` يعطي `border: BORDER_FOCUS` بدون hover effect
  - **`ui_components.py`**: إضافة `card_frame()` — frame مُنسَّق بدون layout مُرفق (الإصلاح الجذري لمشكلة الأزرار الطائرة)
  - **12 ملف** تم تحويل `create_card(self)` المحلية لاستخدام `card_frame()` بدلاً من `create_card()` من ui_components (كان يسبب تعارض Layout)
  - **`payment_management.py`**: تحويل `create_card()` + `style_table()` المحليتين → `card_frame()` + `style_table()` من ui_components؛ إضافة import
  - **`staff_management.py`**: تحويل `create_card()` + `style_table()` المحليتين → `card_frame()` + `style_table()` من ui_components
  - **`academic_settings.py`** + **`communication_ui.py`** + **`student_management.py`**: تحويل `style_table()` المحلية الجزئية → `style_table()` كاملة من ui_components
  - **`finance_payments.py`** line 103: `setStyleSheet(get_table_style())` → `style_table(self.table)` الكاملة
  - **Tabs**: 100% مُركَّزة في جميع الملفات (لا تغييرات مطلوبة)
  - تنظيف: حذف استيرادات `get_table_style` المتبقية (6 ملفات)
- مشاكل واجهتها:
  - الأزرار الطائرة وشريط الأدوات المفقود كانا من نفس السبب: `create_card()` من ui_components تُنشئ QVBoxLayout على الـ frame، فكل Layout ثانٍ يُرفَض من Qt ويصبح بلا parent → الـ widgets تظهر كنوافذ مستقلة
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 4 — نظام الثيم والإعدادات

### الهدف — الوحدة 4

إنشاء إعدادات ثيم قابلة للتبديل وتطبيقها بشكل موحد على التطبيق بأكمله.

### المخرجات المتوقعة — الوحدة 4

- شاشة الواجهة الرئيسية تحتوي على مفتاح `Light / Dark`.
- `ThemeManager` يدير الوضع ويطبقه على `QApplication` و `QDialog`.
- حفظ الوضع المفضل في `config.ini` أو ملف إعداد مركزي.
- قابلية إضافة ثيم مخصص لاحقًا.

### خطوات التنفيذ — الوحدة 4

1. إضافة خيار ثيم في `config.ini` تحت قسم `UI`.
2. قراءة إعداد الثيم عند بدء التطبيق وتطبيقه فوراً.
3. إضافة زر تبديل في نافذة الإعدادات أو `main_dashbord.py`.
4. التحقق من أن جميع النوافذ والحوارات تتغير مع التبديل.

### التحقق والاختبار — الوحدة 4

- [x] اختبار حفظ إعداد الثيم وإعادة تشغيل التطبيق ليتذكّره. ✅
- [x] اختبار التبديل الفوري بين الوضعين في التطبيق. ✅
- [x] اختبار عناصر واجهة المستخدم مثل الأزرار والنصوص في كلا الوضعين. ✅

### مراجعة الوحدة — الوحدة 4

- النتيجة: ✅ مكتملة — `main_dashbord.py` يُجمَّع بلا أخطاء
- ما تم إنجازه:
  - **`config.ini`**: `[APPLICATION] theme` + `[UI] enable_dark_mode` — تخزين الثيم كان موجوداً مسبقاً
  - **`ConfigManager`**: `dark_mode_enabled` يقرأ `[UI] enable_dark_mode` صحيحاً؛ `theme` يقرأ `[APPLICATION] theme`؛ `set()` يكتب التغييرات
  - **`main_dashbord.py`** بدء التطبيق (السطر 106-110): يقرأ `config.dark_mode_enabled` ويطبق الثيم فوراً على النافذة الرئيسية
  - **`btn_theme`** في التوبار: موجود مسبقاً مع أيقونتي ☀️/🌙
  - **`toggle_theme()`**: يحفظ إلى `config.ini`، يطبق على `QApplication.instance()` (يشمل جميع الـ QDialogs)، يحدّث الأيقونة
  - **`apply_runtime_theme_updates()`**: يحدّث sidebar/topbar/content_area/labels/buttons/nav_buttons/user_chip/logout فوراً
  - **`_purge_loaded_modules()`** (جديد): يزيل جميع widgets الوحدات المحمّلة من QStackedWidget ويحررها — سيتم إنشاؤها من جديد بالثيم الجديد عند أول فتح
  - **`_recreate_dashboard()`** (جديد): يزيل ويعيد إنشاء `dashboard_screen` — تُحدَّث بطاقات KPI وبطاقة الرسم البياني وبطاقة الإجراءات السريعة كلها بالألوان الجديدة
  - `toggle_theme()` يستدعي `load_dashboard_data()` بعد إعادة الإنشاء لإعادة رسم البيانات
- مشاكل واجهتها:
  - بطاقات الـ dashboard والوحدات المفتوحة كانت تستخدم أنماطاً صريحة (`setStyleSheet`) تم تطبيقها عند الإنشاء — لا تتأثر بتغيير `QApplication.stylesheet()` وحده
  - الحل: purge + recreate بدلاً من محاولة تحديث كل widget على حدة
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 5 — دليل التصميم المعياري

### الهدف — الوحدة 5

بناء مرجع تصميم داخلي موحّد لكل واجهة التطبيق حتى تكون الإضافات المستقبلية متسقة.

### المخرجات المتوقعة — الوحدة 5

- وثيقة تصميم قصيرة تحتوي على:
  - نظام الألوان.
  - أنماط الأزرار.
  - أنماط الحقول والنماذج.
  - استخدام التبويبات والبطاقات.
  - قواعد الحوارات الموحدة.
- أمثلة عملية لشاشة إدارة الطلاب، شاشة العهدة المالية، شاشة السلوك.
- قائمة مسموحات للتعديلات المحلية والممنوعات.

### خطوات التنفيذ — الوحدة 5

1. تجميع أنماط الواجهة الحالية في فصل منفصل.
2. كتابة أمثلة واضحة عن العناصر المسموح بها.
3. إدراج قسم `لا تستخدم` لتجنب الأنماط المتفرقة.
4. حفظ الوثيقة في ملف يمكن مشاركته مع الفريق.

### التحقق والاختبار — الوحدة 5

- [x] جميع المكونات موثقة مع أمثلة كود قابلة للنسخ. ✅
- [x] قسم ✅/❌ يوضح الأنماط المسموح بها والممنوعة. ✅
- [x] أمثلة من 3 شاشات حقيقية (student_management، finance_payments، student_discipline). ✅

### مراجعة الوحدة — الوحدة 5

- النتيجة: ✅ مكتملة — `DESIGN_GUIDE.md` منشور في جذر المشروع
- ما تم إنجازه:
  - **15 قسماً** تغطي: الألوان، البطاقات، الجداول، التبويبات، الحقول، الأزرار، الفواصل، الحوارات، نوافذ الوحدات، KPI، أخطاء DB، PDF
  - **جدول ✅/❌** مع السبب والبديل لكل نمط ممنوع (8 أنماط ممنوعة موثقة)
  - **3 أمثلة شاشات حقيقية** بكود جاهز للنسخ
  - **قسم الاستيرادات المعيارية** — كل ما يحتاجه المطوّر في مكان واحد
  - تحذير `card_frame` vs `create_card` موثق مع شرح bug الأزرار الطائرة
- مشاكل واجهتها: لا شيء
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 6 — تحسين الوصول وقابلية الاستخدام

### الهدف — الوحدة 6

رفع جودة تجربة المستخدم وتأمين الوصول للواجهات بشكل أفضل.

### المخرجات المتوقعة — الوحدة 6

- تحسين التركيز والترتيب الأوتوماتيكي في الحقول.
- تعزيز التباين بين النصوص والخلفيات.
- دعم العناصر الأساسية في الكيبورد.
- قائمة فحص وصول بسيطة.

### خطوات التنفيذ — الوحدة 6

1. مراجعة ألوان النص والخلفية عبر `ui_styles.py` لفحص التباين.
2. إضافة تركيز واضح للعناصر باستخدام CSS أو `QFocusFrame`.
3. التحقق من التاب والترتيب في الحوارات والنماذج.
4. اختبار حالات الكتابة من لوحة المفاتيح وحدات الإدخال.

### التحقق والاختبار — الوحدة 6

- [x] مؤشر التركيز مرئي على الأزرار عند التنقل بـ Tab. ✅
- [x] مفتاح Enter يفعّل الزر الافتراضي في الحوارات. ✅
- [x] الجداول قابلة للتنقل بالأسهم بعد `StrongFocus`. ✅
- [x] الحوارات تضع المؤشر في أول حقل تلقائياً عند الفتح. ✅
- [x] 1047 اختبار ناجح — لا تراجع. ✅

### مراجعة الوحدة — الوحدة 6

- النتيجة: ✅ مكتملة — `ui_components.py` يُجمَّع بلا أخطاء، 1047 اختبار ناجح
- ما تم إنجازه:
  - **`styled_button()`**: إضافة `QPushButton:focus` (حدود BORDER_FOCUS زرقاء) + `QPushButton:pressed` — مؤشر مرئي لمستخدمي لوحة المفاتيح؛ الحدود الأساسية أصبحت `2px solid transparent` للحفاظ على الحجم الثابت عند ظهور حدود التركيز
  - **`dialog_button_row()`**: `ok_btn.setDefault(True)` + `cancel_btn.setAutoDefault(False)` — مفتاح Enter يُفعّل الزر الأخضر دون الحاجة للنقر
  - **`style_table()`**: `table.setFocusPolicy(Qt.FocusPolicy.StrongFocus)` — الجداول تستقبل التركيز وتستجيب لمفاتيح الأسهم
  - **`BaseDialog.showEvent()`**: يستدعي `_focus_first_input()` عبر `QTimer.singleShot(0)` لضمان وضع المؤشر في أول حقل قابل للتعديل عند فتح الحوار
  - **`set_tab_order(widgets)`**: دالة مساعدة جديدة لتسلسل ترتيب Tab عبر قائمة من الـ widgets
  - **استيراد `QTimer`**: أُضيف للـ imports
  - حقول الإدخال كانت بالفعل مكتملة (`:focus` موجود على كل منها) — لا تغييرات مطلوبة
- مشاكل واجهتها:
  - شرط التحقق في `_focus_first_input` كان ملتوياً — أُعيد كتابته بـ `continue` للتوضيح
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## الوحدة 7 — مراجعة جودة التكامل

### الهدف — الوحدة 7

التأكد من أن التغييرات لا تضر بالتطبيق الكامل وأن النظام قابل للصيانة.

### المخرجات المتوقعة — الوحدة 7

- قائمة اختبارات يدوية لكل شاشة رئيسية.
- مجموعة من حالات الاختبار المكتوبة إن أمكن.
- مراجعة الأكواد للتأكد من إزالة التكرار.

### خطوات التنفيذ — الوحدة 7

1. إجراء مراجعة نهائية للشاشات الرئيسية بعد تطبيق الوحدات السابقة.
2. إجراء اختبار تكامل يدوي على أجزاء التطبيق المتأثرة.
3. كتابة حالات اختبار `pytest` إن وجدت أدوات الدعم.
4. تحديث هذا الملف بنتائج كل خطوة.

### التحقق والاختبار — الوحدة 7

- [x] `py_compile` ناجح على جميع الملفات المعدلة (24 ملف). ✅
- [x] 1047 اختبار ناجح — لا تراجع. ✅
- [x] مراجعة أنماط التكرار: `get_table_style` مركزة 100%، بطاقات `card_frame` موحدة. ✅
- [x] استيرادات orphaned حذفت من 3 ملفات. ✅
- [x] تكرار PDF helpers أُزيل من `admin_documents.py` و`pdf_report_style.py`. ✅

### مراجعة الوحدة — الوحدة 7

- النتيجة: ✅ مكتملة — 1047 اختبار ناجح، جميع الملفات تُجمَّع بلا أخطاء
- ما تم إنجازه:
  - **مراجعة شاملة** لأنماط التكرار المتبقية بعد الوحدات 1-6
  - **`admin_documents.py`**: 4 دوال محلية (`_get_arabic_font_path`، `_contains_arabic`، `_prepare_pdf_text`، `_sanitize_latin`) كانت تُظلّل imports من `pdf_helpers` — تم الحذف وتصحيح alias الـ`sanitize_latin`
  - **`pdf_report_style.py`**: دالة `_sanitize_latin` محلية → import من `pdf_helpers`
  - **استيرادات orphaned محذوفة**: `get_table_style` كان مستورداً دون استخدام في `finance_dashboard.py`، `payment_management.py`، `system_maintenance.py`
  - **التروك المقصودة**: wrapper methods في `student_management.py` و`advanced_reports.py` (تفويض 1 سطر) لم تُحذف — تغيير call sites بدون فائدة معمارية
- مشاكل واجهتها:
  - `admin_documents.py` كانت تستورد `sanitize_latin as sanitize` لكن الكود يستخدم `_sanitize_latin` — تصحيح الـ alias أحل التعارض
- الإصلاحات المطلوبة: لا شيء — الوحدة مكتملة 100%

---

## ملاحظات عامة

- يتم تحديث هذا الملف في نهاية كل وحدة، مع تسجيل تاريخ الإنجاز والملاحظات.
- إذا ظهرت مشكلة كبيرة في أي وحدة، لا تنتقل للوحدة التالية حتى تُغلَق.
- الهدف النهائي: واجهة واحدة متناسقة، قابلة للصيانة، وجاهزة لتطوير أسرع.
