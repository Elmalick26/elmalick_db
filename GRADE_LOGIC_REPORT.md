# تقرير مراجعة منطق الحساب المدرسي — El Malick Gest

| | |
|---|---|
| **النطاق** | صحة منطق الحساب البيداغوجي (المعدلات، الرتبة، الانتقال، الإنقاذ) |
| **الفرع** | `qa/grade-logic-review` → مدموج في `main` (`8ec708e`) |
| **قاعدة الاختبار** | `elmalick_test` (منفصلة، بلا بيانات إنتاج؛ `elmalick_db` لم تُمسّ) |
| **الاختبارات** | 1171 ناجحاً (ترتيب عشوائي + حتمي) |
| **عدد الـcommits** | 6 |

---

## 1. القواعد المرجعية المعتمَدة

**الإعدادي/الثانوي (فصلان، /20)**: معدل الفروض = (D1+D2)/2 ؛ معدل المادة = (معدل الفروض + التركيبة)/2 ؛ المعدل العام = Σ(معدل المادة × المعامل) ÷ Σ(المعاملات) ؛ النجاح ≥ 10 مع إمكان «إنقاذ» يدوي للمدير موثّق.

**الابتدائي (3 فصول، /10)**: لا فروض — التركيبة فقط هي معدل المادة ؛ المعدل العام = Σ(التركيبة × المعامل) ÷ Σ(المعاملات).

---

## 2. الاكتشافات والإصلاحات (بأمثلة رقمية محسوبة)

| المعرّف | الخطورة | المشكلة | الإثبات الرقمي | الإصلاح | Commit |
|---------|:------:|---------|----------------|---------|--------|
| **G1** | 🔴 | معدل المادة كان `(معدل الفروض + تركيبة×2)/3` (التركيبة بوزن مضاعف) | D1=12,D2=14,C=10: الكود **11.0** بدل **11.5** | الصيغة الصحيحة `(معدل الفروض + تركيبة)/2` | `a9739e6` |
| **G2** | 🔴 | كشف المرحلة بمطابقة نصية حسّاسة للّكنات: «Élémentaire» (بـé) لا يُكتشف كابتدائي → يُعامَل إعدادياً (/20 وصيغة الفروض) | `is_primary("Élémentaire")` كان False | دالة مشتركة `is_primary_cycle` تطبّع اللكنات + العربية | `a9739e6` |
| **G3** | 🔴 | الابتدائي كان يُدخل الفروض في معدل المادة | تركيبة=7 وفرض=1 → 5.0 بدل 7.0 | الابتدائي = التركيبة فقط | `a9739e6` |
| **G2-ب** | 🟠 | نفس نمط الكشف مكرَّر في 5 مواضع (Python + SQL) بنفس الهشاشة | — | توحيدها على `is_primary_cycle` + توسيع regSQL لـ`élém` | `69e28e1` |
| **H1** | 🔴 | تعذّر بناء المخطط من الصفر: `StudentAttendance`/`StudentDiscipline` (FK→Timetable) تُنشأ **قبل** `Timetable` | فشل التثبيت على قاعدة فارغة | نقل `Timetable` قبل تابعيه — 31 جدولاً تُبنى الآن | `627bd63` |
| **rachat** | 🟠 | التجاوز اليدوي لقرار الترقية مأذون (مدير) لكن **غير موثّق** | لا سجل تدقيق للتجاوزات | تسجيل كل تجاوز في `AuditLogs` (الفاعل + الطالب + القيمة القديمة→الجديدة) + تمرير المستخدم للوحدات | `7049985` |
| **G6** | 🔴 | **حساب مزدوج**: قرار الترقية يستخدم `AVG` مسطّحاً `(D1+D2+C)/3` مخالفاً للكشف | كشف **11.5** / ترقية **12.0** | مصدر واحد `GradeService.subject_average()` يستخدمه المساران | `b64e436` |
| **G4** | 🟡 | معالجة المفقود متباينة (الكشف=0 / الترقية تستثني) | — | توحيد على **المفقود=0** (قرارك) في المسارين | `8ec708e` |

## 3. التحقق التكاملي (على القاعدة الحقيقية المعزولة)

| السيناريو | النتيجة |
|-----------|---------|
| إعدادي E2E: D1=12,D2=14,C=10 → مادة 11.5 ؛ معدل عام (معاملات 4،2) = 11.1667 | ✅ |
| ابتدائي E2E: «Élémentaire»→/10، تركيبة فقط، (7×6+8×4)/10 = 7.4 | ✅ |
| **الكشف == قرار الترقية** = 11.1667 (بعد G6) | ✅ |
| المفقود=0 في المسارين (فرض مفقود) → كلاهما 8.0 (بعد G4) | ✅ |
| رفض درجة سالبة / >20 (قيد القاعدة) | ✅ |
| Unicode (أسماء فرنسية + عربية) دورة كاملة | ✅ |
| بناء المخطط من الصفر (بعد H1) | ✅ 31 جدولاً |

## 4. المصدر الواحد للحقيقة (بعد التوحيد)

`services/grade_service.py`:
- `subject_average(devoirs, composition, is_primary)` — معدل المادة القانوني.
- `is_devoir_assessment(type_code, name)` — تصنيف فرض/تركيبة.
- `is_primary_cycle(name)` — كشف المرحلة (لكنات + عربية).

يستخدمها **الكشف** (`bulletin_generation`) و**قرار الترقية** (`year_end_migration`) معاً → لا تضارب.

## 5. بنود متبقّية موثّقة (خارج صميم منطق الحساب)

| # | الوصف | الحالة |
|---|-------|--------|
| **G5** 🟡 | قيد القاعدة `0..20` لا يفرّق /10 للابتدائي | **مُغلق عند الإدخال**: الواجهة تفرض `setRange(0, max_score)` بـ10 للابتدائي (بعد G2-ب)؛ يبقى قيد القاعدة backstop خشن (CHECK لا يعرف المرحلة دون join) |
| **H2** 🔴 | سلسلة alembic غير مكتفية ذاتياً (التثبيت النظيف عبر alembic يفشل؛ يعتمد على `db_schema`) | **مسألة نشر (المرحلة 7)**؛ refactor كبير منفصل — يُنصح بجعل الهجرة 001 تُنشئ كل الجداول، أو توثيق مسار `db_schema + alembic stamp head` |

## 6. سجلّ الـcommits

```
a9739e6  fix(qa): correct collège subject-average formula + robust cycle detection (G1/G2/G3)
69e28e1  refactor(qa): unify primary-cycle detection on accent-robust helper (G2-b)
627bd63  fix(qa): create Timetable before its dependents in db_schema (H1)
7049985  feat(qa): audit manual promotion overrides / rachat
b64e436  fix(qa): unify subject-average across bulletin and promotion (G6)
8ec708e  fix(qa): missing assessment counts as 0 in promotion path too (G4)
```

## 7. التحقق (إعادة الإنتاج)

```bash
# اختبارات منطق الحساب البيداغوجي
.venv/Scripts/python -m pytest tests/test_grade_calc_pedagogy.py tests/test_cycle_detection.py \
    tests/test_rachat_audit.py tests/test_schema_table_order.py -v

# المجموعة الكاملة (خضراء في كلا الترتيبين)
.venv/Scripts/python -m pytest                 # 1171 passed
.venv/Scripts/python -m pytest -p no:randomly  # 1171 passed
```

---

*كل بنود منطق الحساب الحرجة والمتوسطة عولِجت وتُحقّق منها وحدةً وتكاملاً على قاعدة معزولة. G5 مُغلق عند الإدخال؛ H2 مسألة نشر منفصلة.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
